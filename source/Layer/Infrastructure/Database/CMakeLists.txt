file(GLOB MEMLY_DATABASE_MODULE_INTERFACES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/*.mpp")
file(GLOB MEMLY_DATABASE_MODULE_IMPLEMENTATION_UNITS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/*.cpp")
file(GLOB MEMLY_DATABASE_INTERNAL_MODULE_INTERFACES CONFIGURE_DEPENDS
     "${CMAKE_CURRENT_SOURCE_DIR}/module/Internal/*.mpp"
)
file(GLOB MEMLY_DATABASE_INTERNAL_MODULE_IMPLEMENTATION_UNITS CONFIGURE_DEPENDS
     "${CMAKE_CURRENT_SOURCE_DIR}/module/Internal/*.cpp"
)
file(GLOB MEMLY_DATABASE_BOOTSTRAP_SQL CONFIGURE_DEPENDS
     "${CMAKE_CURRENT_SOURCE_DIR}/module/Internal/Sql/Bootstrap/*.sql"
)
file(GLOB MEMLY_DATABASE_INSERT_SQL CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/Internal/Sql/Insert/*.sql")
file(GLOB MEMLY_DATABASE_SELECT_SQL CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/Internal/Sql/Select/*.sql")
file(GLOB MEMLY_DATABASE_VERSION_SQL CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/Internal/Sql/Version/*.sql")

memly_add_component(
  Memly.Database
  Memly.Database
  PUBLIC_MODULE_INTERFACES
  ${MEMLY_DATABASE_MODULE_INTERFACES}
  INTERNAL_MODULE_INTERFACES
  ${MEMLY_DATABASE_INTERNAL_MODULE_INTERFACES}
  SOURCES
  ${MEMLY_DATABASE_MODULE_IMPLEMENTATION_UNITS}
  ${MEMLY_DATABASE_INTERNAL_MODULE_IMPLEMENTATION_UNITS}
)
memly_configure_duckdb_header(Memly.Database)

qt_add_resources(
  Memly.Database
  "DatabaseMigrationSql"
  PREFIX
  "/Sql"
  BASE
  "${CMAKE_CURRENT_SOURCE_DIR}/module/Internal/Sql"
  FILES
  ${MEMLY_DATABASE_BOOTSTRAP_SQL}
  ${MEMLY_DATABASE_INSERT_SQL}
  ${MEMLY_DATABASE_SELECT_SQL}
  ${MEMLY_DATABASE_VERSION_SQL}
)
memly_link_component_dependencies(Memly.Database PUBLIC Memly.Exception PRIVATE Memly.QtApp duckdb)
