file(GLOB MEMLY_BRIDGE_MODULE_INTERFACES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/*.mpp")
file(GLOB MEMLY_BRIDGE_MODULE_IMPLEMENTATION_UNITS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/*.cpp")

memly_add_component(
  Memly.Bridge Memly.Bridge PUBLIC_MODULE_INTERFACES ${MEMLY_BRIDGE_MODULE_INTERFACES} SOURCES
  ${MEMLY_BRIDGE_MODULE_IMPLEMENTATION_UNITS}
)
memly_link_component_dependencies(Memly.Bridge PUBLIC Qt6::Quick)
