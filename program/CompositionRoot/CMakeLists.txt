file(GLOB MEMLY_COMPOSITION_ROOT_EXPORTED_MODULES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/export/*.cppm")
file(GLOB MEMLY_COMPOSITION_ROOT_IMPLEMENTATION_UNITS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/source/*.cpp")

memly_add_component(
  Memly.CompositionRoot Memly.CompositionRoot PUBLIC_MODULES ${MEMLY_COMPOSITION_ROOT_EXPORTED_MODULES} SOURCES
  ${MEMLY_COMPOSITION_ROOT_IMPLEMENTATION_UNITS}
)
memly_link_component_dependencies(Memly.CompositionRoot PUBLIC Memly.Database Memly.Repository Memly.Service)
