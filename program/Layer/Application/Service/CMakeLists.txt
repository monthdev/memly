file(GLOB MEMLY_SERVICE_MODULE_INTERFACES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/*.cppm")
file(GLOB MEMLY_SERVICE_MODULE_IMPLEMENTATION_UNITS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/module/*.cpp")

memly_add_component(
  Memly.Service Memly.Service PUBLIC_MODULE_INTERFACES ${MEMLY_SERVICE_MODULE_INTERFACES} SOURCES
  ${MEMLY_SERVICE_MODULE_IMPLEMENTATION_UNITS}
)
memly_link_component_dependencies(Memly.Service PUBLIC Memly.IndexCache Memly.Repository PRIVATE Memly.Domain)
