file(GLOB MEMLY_INVALIDATION_MODULE_INTERFACES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/export/*.cppm")
file(GLOB MEMLY_INVALIDATION_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/source/*.cpp")

memly_add_component_module(
  memly_application_invalidation Memly.Invalidation INTERFACES ${MEMLY_INVALIDATION_MODULE_INTERFACES} SOURCES
  ${MEMLY_INVALIDATION_SOURCES}
)
