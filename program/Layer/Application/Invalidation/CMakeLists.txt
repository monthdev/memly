file(GLOB MEMLY_INVALIDATION_EXPORTED_MODULES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/export/*.cppm")
file(GLOB MEMLY_INVALIDATION_IMPLEMENTATION_UNITS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/source/*.cpp")

memly_add_component(
  Memly.Invalidation Memly.Invalidation PUBLIC_MODULES ${MEMLY_INVALIDATION_EXPORTED_MODULES} SOURCES
  ${MEMLY_INVALIDATION_IMPLEMENTATION_UNITS}
)
memly_link_component_dependencies(Memly.Invalidation PUBLIC Qt6::Core)
