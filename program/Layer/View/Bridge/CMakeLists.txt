file(GLOB MEMLY_BRIDGE_EXPORTED_MODULES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/export/*.cppm")
file(GLOB MEMLY_BRIDGE_IMPLEMENTATION_UNITS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/source/*.cpp")

memly_add_component(
  Memly.Bridge Memly.Bridge PUBLIC_MODULES ${MEMLY_BRIDGE_EXPORTED_MODULES} SOURCES
  ${MEMLY_BRIDGE_IMPLEMENTATION_UNITS}
)
memly_link_component_dependencies(Memly.Bridge PUBLIC Qt6::Quick)
