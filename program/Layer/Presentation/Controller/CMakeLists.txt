file(GLOB MEMLY_CONTROLLER_EXPORTED_MODULES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/export/*.cppm")
file(GLOB MEMLY_CONTROLLER_IMPLEMENTATION_UNITS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/source/*.cpp")

memly_add_component(
  Memly.Controller Memly.Controller PUBLIC_MODULES ${MEMLY_CONTROLLER_EXPORTED_MODULES} SOURCES
  ${MEMLY_CONTROLLER_IMPLEMENTATION_UNITS}
)
memly_link_component_dependencies(Memly.Controller PUBLIC Qt6::Core)
